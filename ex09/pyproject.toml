[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"
authors = [
    {name = "yberrim", email = "yberrim@42.fr"}
]
license = {text = "MIT"}
readme = "README.md"

[project.urls]
Homepage = "https://github.com/yassineberrim/ft_package"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"